cmake_minimum_required(VERSION 3.12)
aux_source_directory(./src VM_SRC)
aux_source_directory(./src/type VM_SRC_TYPE)
include_directories(./include)
add_library(${NEO_PROJECT_NAME}-vm SHARED ${VM_SRC} ${VM_SRC_TYPE})
