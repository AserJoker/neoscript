cmake_minimum_required(VERSION 3.12)
aux_source_directory(./src ENGINE_SRC)
aux_source_directory(./src/type ENGINE_SRC_TYPE)
include_directories(./include)
add_library(${NEO_PROJECT_NAME}-engine STATIC ${ENGINE_SRC} ${ENGINE_SRC_TYPE})